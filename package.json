{
  "name": "task-management",
  "version": "1.0.0",
  "description": "Currently only the frontend is working, and its running on static data in memory, backend has not yet been properly developed and connected to frontend.",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
